# Python dependencies for development environment
# Note: This is a frontend React project, but these are helpers for dev environment

# Virtual environment and package management
virtualenv==20.21.0
wheel==0.38.4

# Linting and code quality
black==23.3.0
pylint==2.17.4

# API helpers (if needed for backend integration)
requests==2.31.0
python-dotenv==1.0.0

# Testing
pytest==7.3.1

# Node.js is required but installed separately
# Required npm packages are managed via package.json 